// Generated by CoffeeScript 1.7.1
var DidGetUsersData;

$(document).on("deviceready", function() {
  var nickname;
  $(".actionbar").remove();
  nickname = getQueryString("nickname");
  RequestAjax("GET", "/mj/article/" + nickname, {}, DidGetUsersData, null);
});

DidGetUsersData = function(data, rawData) {
  var htmlString, usersHTML;
  usersHTML = template("People/Followings");
  htmlString = usersHTML(data.Data);
  if (htmlString.search("Template Error") < 0) {
    $(".container").replaceWith(htmlString);
  } else {
    ShowLoadingError();
    return;
  }
  $(".actionbar .page-title").text("");
  CenterTitle();
};
